{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            //"program": "${workspaceFolder}/bin/${fileBasenameNoExtension}",
            "program": "${workspaceFolder}/bin/main",
            "args": ["${input:graphFile}"],
            "cwd": "${workspaceFolder}"
        }
    ],
    "inputs": [
    {
        "id": "graphFile",
        "type": "promptString",
        "description": "Path to graph input file"
    }
]
}